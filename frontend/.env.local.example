# Copy to .env.local. Only NEXT_PUBLIC_* values reach the browser, so nothing secret
# belongs in this file.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Optional: use MapTiler's Outdoor vector basemap instead of the keyless USGS topo tiles.
# NEXT_PUBLIC_MAPTILER_KEY=
